flask==3.0.0
gunicorn==21.2.0
# outras dependências...
```

**Procfile** (crie este arquivo - importante!):
```
web: gunicorn app:app
```
*Substitua `app:app` por `seu_arquivo:nome_da_variavel_flask`*

**runtime.txt** (opcional, mas recomendado):
```
python-3.11.0
